[project]
name = "enterprise-agent-api"
version = "0.1.0"
description = "Stateful Agent API with LangGraph and Redis"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn>=0.30.1",
    "langgraph>=0.1.0",
    "langgraph-checkpoint-redis>=1.0.4",
    "langchain-openai>=0.1.0",
    "prometheus-fastapi-instrumentator>=7.0.0",
    "redis>=5.0.0"
]
